# hypotenus of the right anled triangle
import math
a=float(input("Enter the base of the right-angled triangle:"))
b=float(input("Enter the height of the right-angled triangle:"))

c=math.sqrt(math.pow(a,2)+math.pow(b,2))
print(c)